<!doctype html>
<html lang="{{ lang or 'it' }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} · {% endif %}{{ site.legalName }}</title>
  <meta name="description" content="{{ description or site.tagline }}">
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">
  {# OpenGraph / social sharing #}
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="{{ site.legalName }}">
  <meta property="og:title" content="{% if title %}{{ title }} · {% endif %}{{ site.legalName }}">
  <meta property="og:description" content="{{ description or site.tagline }}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:image" content="{{ site.url }}/assets/img/logo.png">
  <meta property="og:locale" content="{% if (lang or 'it') == 'en' %}en_US{% else %}it_IT{% endif %}">
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="{% if title %}{{ title }} · {% endif %}{{ site.legalName }}">
  <meta name="twitter:description" content="{{ description or site.tagline }}">
  <meta name="twitter:image" content="{{ site.url }}/assets/img/logo.png">
  <link rel="icon" type="image/png" href="/assets/img/logo-128.png">
  <link rel="preload" href="/assets/fonts/inter-400.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="/assets/fonts/dmserif-400.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="stylesheet" href="/assets/css/style.css">
  {% set altLinks = page.url | altLinks(collections.all) %}
  {% if altLinks.length %}
  <link rel="alternate" hreflang="{{ lang or 'it' }}" href="{{ site.url }}{{ page.url }}">
  {% for l in altLinks %}<link rel="alternate" hreflang="{{ l.lang }}" href="{{ site.url }}{{ l.url }}">
  {% endfor %}<link rel="alternate" hreflang="x-default" href="{{ site.url }}{% if (lang or 'it') == 'it' %}{{ page.url }}{% else %}{{ altLinks[0].url }}{% endif %}">
  {% endif %}
</head>
<body>
  <a href="#main-content" class="skip-link">Salta al contenuto</a>
  {% include "partials/nav.njk" %}
  <main id="main-content">
    {% if not hideHeader %}
    <header class="page-header">
      <div class="hero-dots"></div>
      <div class="inner">
        {% if eyebrow %}<div class="eyebrow">{{ eyebrow }}</div>{% endif %}
        <h1>{{ (heading or title) | safe }}</h1>
        {% if lead %}<p class="lead">{{ lead | safe }}</p>{% endif %}
      </div>
    </header>
    {% endif %}
    {{ content | safe }}
  </main>
  {% include "partials/footer.njk" %}
  {% if site.cloudflareAnalyticsToken %}
  <script defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": "{{ site.cloudflareAnalyticsToken }}"}'></script>
  {% endif %}
  <script src="/assets/js/lang-cookie.js"></script>
</body>
</html>
